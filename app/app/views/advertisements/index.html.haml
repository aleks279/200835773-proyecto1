- @advertisements.in_groups_of(3).each do |property_group|
  .row.spacing-top
    - property_group.each do |property|
      - if property
        .col-md-1
        .col-md-2
          
          = link_to "#{image_tag('property-placeholder.png')}".html_safe, property
          .col-md-12
            
